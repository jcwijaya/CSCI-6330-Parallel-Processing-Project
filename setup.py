from setuptools import find_packages, setup

setup(
    name='parallelpixie',
    packages=find_packages(),
    version='0.1.0',
    description='A pandas parallel processing library.',
    author='Freyja Richardson',
)